[pytest]
# Agent: default CI-safe suite. See .cursor/rules/pymolviz-testing.mdc
testpaths = tests/unit tests/runtime
pythonpath = module .
addopts = -ra
markers =
    pymol: requires real PyMOL (opt-in)
    qt: requires PyMOL Qt (opt-in)
